type Post {
  id: ID! @id
  createdAt: DateTime @createdAt
  updatedAt: DateTime @updatedAt
  content: String!
  image: String!
  postedBy: User
}

type User {
  id: ID! @id
  createdAt: DateTime @createdAt
  username: String!
  email: String! @unique
  password: String!
  posts: [ Post! ]
}